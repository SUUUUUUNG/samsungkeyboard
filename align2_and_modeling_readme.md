# 정합 수정 + 엄지 길이 예측 모델 (P1·P15)

## 결론 요약
- **정합(Phase A)**: 이전 정합이 손 축 방향으로 5~9mm 덜 밀린 국소 최소점에 빠져 있던 것을 고쳤습니다. 15명 모두 실측 19점의 평균 표면 거리 0.17~0.28mm, P1과 메쉬 엄지 끝점의 간격 0.05~1.83mm입니다. 엄지 길이 값(`thumb_length.csv`)은 z 회전+이동만 쓰므로 그대로입니다.
- **모델(Phase B)**: 메쉬만으로 P1·P15를 찍고 **xy 평면** 길이를 내는 모델을 만들었습니다. 12명 LOO에서 길이 MAE **1.87mm**(4/12명이 0.5mm 이내), 테스트 3명은 **+2.0 / +3.4 / −3.6mm**. 15명 전체를 학습에 없는 상태로 예측한 MAE는 **2.10mm**(중앙값 1.91). **목표 0.5mm에 도달하지 못했습니다.** 테스트 3명 수치는 어느 3명을 뽑느냐에 따라 0.2~4.6mm로 흔들리므로 15명 전체 수치로 보는 것이 맞습니다.
- **원인은 P15(엄지 기저, MCP 주름)** 입니다. P1(엄지 끝)은 축 방향 오차 표준편차 0.59mm로 목표 수준이지만, P15는 표준편차 2.5mm입니다. P15는 유리판에 눌린 평평한 손바닥 위에 있어 국소 형상 단서가 없고, 보이는 기하(엄지 끝점·엄지-검지 오목점·손목)에 대한 상대 위치가 사람마다 ±5mm씩 다릅니다. 시도한 방법(전역 회귀, 아틀라스 변형 9종, 관절 비율, 방법 블렌드) 어느 것도 P15를 1.8mm MAE 아래로 못 내렸습니다.

## lnd 랜드마크의 구조 (15명 전부 오차 0.000mm로 확인)
lnd는 비공개 SW가 3D 스캔 위에서 찍은 점과, 그로부터 계산으로 채운 점으로 나뉩니다.
- **3D로 찍은 점 19개**: 1~6, 15~27 (z가 사람마다 다른 실측값)
- **계산으로 채운 점 9개**: 7~14는 각 손가락의 끝점(P2~P5)→밑점(P16~P19) 직선의 1/3·2/3 지점(xy만 계산, z=0), P28은 P26·P27의 중점(z=1).

| 손가락 | 끝 | 1/3 | 2/3 | 밑 |
|---|---|---|---|---|
| 검지 | P2 | P7 | P11 | P16 |
| 중지 | P3 | P8 | P12 | P17 |
| 약지 | P4 | P9 | P13 | P18 |
| 소지 | P5 | P10 | P14 | P19 |

P15와 P6은 다른 점들의 내분점으로 설명되지 않는(최선 조합도 잔차 4~6mm) 독립적으로 찍힌 점입니다. P1·P6·P15는 거의 일직선(평균 1.2°)이지만 P6의 위치 비율은 사람마다 달라 고정 규칙이 아닙니다. SW가 P15를 어떤 정보로 잡았는지(형상만인지, 텍스처·템플릿·수동 클릭인지)는 이 데이터로 알 수 없습니다.

## Phase A — 정합 수정 (`align_landmarks.py`, 완료)

### 문제
손가락은 축 방향으로 긴 튜브라, lnd 전체가 손가락 방향으로 몇 mm 미끄러져도 점–표면 거리는 거의 변하지 않습니다(끝점을 넘어가기 전까지). 이전 버전의 trimmed ICP는 매 단계 가장 안 맞는 15%(19점 중 3점)를 버렸는데, 그 3점이 정확히 밀어줘야 할 P20~22였습니다. 그 결과 0004·2633·2634·1115 등에서 손가락 끝 랜드마크가 메쉬의 손가락 끝보다 5~9mm 안쪽에 놓였고, 이 오차는 표면 거리로는 보이지 않았습니다.

### 수정
1. `TRIM_RATIO = 1.0` — 정합점 19개(실측 3D 점)를 매 단계 전부 사용.
2. `hand_axis(V)` — 메쉬만으로 손 축을 정의(손목 절단면 y<ymin+3의 xy 중심 → 최대 y 정점). lnd의 손 축(P28→P3)과 0.5~3° 이내로 일치.
3. 각도 스윕으로 고른 해에서 손 축 방향으로 −6~+16mm(2mm 간격) 옮긴 시작점마다 ICP를 다시 돌리고, 정합점 평균 거리가 가장 작은 해를 채택.
4. `summary.csv`에 `mean_dist_fitted_mm`, `tip_apex_gap_mm`(P1에서 엄지 축 방향 최원 정점까지의 축 방향 거리) 추가.

### 결과 (15명)
| 지표 | 이전 | 현재 |
|---|---|---|
| 정합점 평균 거리 | 0.32~2.77mm | **0.17~0.28mm** |
| 정합점 최대 거리 | 최대 8.3mm | **≤0.65mm** |
| P1–엄지 끝점 축 방향 간격 | 0.3~9.2mm (표준편차 2.9) | **0.05~1.83mm (평균 0.9, 표준편차 0.5)** |
| z 회전각 | −0.5~6.4° | 0.5~4.6° |
| 엄지 길이 vs 기준값 | 15/15 일치 | 15/15 일치 (값 불변) |

"메쉬 엄지 길이(P15→엄지 끝점) − 기준값"도 평균 4.44±2.87mm에서 0.91±0.50mm로 줄었습니다. 즉 lnd 정답은 메쉬와 일관되고, 잘못됐던 것은 정합이었습니다.

## Phase B — 모델 (`thumb_model/`)

### 구성
| 파일 | 역할 |
|---|---|
| `prep.py` | 메쉬 → 공통 좌표계(손목 중심 원점, +y = 손 축) → 정점별 특징 32개 + 라벨 → `cache/<name>.npz` |
| `train_eval.py` | 학습, 12명 LOO 교차검증, 방법 선택, 3명 테스트, `model_<stage1>.pkl` 저장 |
| `predict.py` | `python thumb_model/predict.py hand.obj` → P1·P15(obj 좌표계)와 길이 출력, `predictions/<name>_pred_landmarks.ply` |

정점별 특징(32개): 공통 좌표계 xyz와 손 길이로 나눈 xy, 법선, k=12/40/120 최근접 이웃(≈반경 3/6/10mm)의 PCA 고유값 비율·법선 평균 내적·접평면 대비 높이·반경, xy 기둥 두께와 상하 여유, 최소 x·최대 x·최대 y 정점·메쉬 중심·손목까지 거리, 손목 기준 극각. 계획서의 증강(회전·이동)은 병목이 특징이 아니라 P15 자체여서 넣지 않았습니다.

**1단계(거친 위치)**: 정점마다 "랜드마크까지 거리(40mm에서 클립)"를 `HistGradientBoostingRegressor`로 회귀(메쉬당 무작위 6000점 + 랜드마크 25mm 이내 전부). 예측 거리 상위 50개 정점의 (좌표 + 오프셋 회귀값)을 가중 평균.
**2단계(정밀화)**, LOO로 비교해 랜드마크별로 선택:
- `fine`: 거친 위치 15mm 이내에서 σ=2mm 규모의 거리·오프셋 재회귀
- `atlas`: 학습 손마다 정답 주변 패치(P1 20mm, P15 30mm)를 잘라 테스트 손에 유사변환 ICP(스케일 0.85~1.15)로 맞추고 정답을 옮김 → 잔차 가중 중앙값
- `apex`(P1만): 엄지 축 튜브에서 최원 표면점(끝점)을 찾고, 학습 손에서 구한 끝점→P1 평균 오프셋만큼 되돌림

테스트 3명(`TEST_SUBJECTS`): `20_F_0179G`, `20_F_2635G`, `20_M_1116G`. 나머지 12명으로 LOO.

### 결과 — 12명 LOO (길이 오차, mm; `results_cv_hgb_summary.csv`)
| P1 | P15 | MAE | 편향 | 최대 | ≤0.5mm | P1 3D오차 | P15 3D오차 |
|---|---|---|---|---|---|---|---|
| **apex** | **atlas** | **1.87** | −0.43 | 4.99 | 4/12 | 1.97 | 2.36 |
| fine | atlas | 1.89 | −0.29 | 4.92 | 3/12 | 2.24 | 2.36 |
| coarse | atlas | 1.91 | −0.30 | 4.62 | 3/12 | 2.06 | 2.36 |
| apex | fine | 2.57 | −0.98 | 6.97 | 3/12 | 1.97 | 3.33 |
| coarse | coarse | 2.78 | −0.75 | 6.86 | 0/12 | 2.06 | 3.26 |

채택: P1 = apex, P15 = atlas. 사람별 길이 오차(0004, 0097, 2633, 2634, 2636, 2637, 1113, 1114, 1115, 1117, 1559, 1573 순): +1.1, −5.0, −0.2, +1.3, +0.4, −0.1, −4.5, −1.9, −2.0, +1.3, +0.5, +4.2. 블렌드 방법(`mean4`, `apex+atlas`, `coarse+atlas`, `fine+atlas`, `mean3`)은 모두 1.88~2.36으로 더 낫지 않았습니다.

LOO에서 구한 편향 보정(−0.43mm)은 테스트에서는 오히려 오차를 키웠습니다(보정 전 MAE 3.01 → 보정 후 3.15). 12명으로 추정한 편향은 신뢰할 수 없다는 뜻이라, 아래 테스트 표는 보정 전 값입니다.

### 결과 — 테스트 3명 (12명으로 학습, `results_test_hgb.csv`)
| 사람 | 예측 | 기준값 | 오차 | P1 오차 | P15 오차 |
|---|---|---|---|---|---|
| 20_F_0179G | 47.62 | 45.6 | **+2.02** | 1.84 | 2.17 |
| 20_F_2635G | 48.04 | 44.6 | **+3.44** | 0.62 | 3.60 |
| 20_M_1116G | 51.53 | 55.1 | **−3.57** | 2.07 | 3.14 |

### 오차 분해 — 왜 P15인가
| | 축 방향 오차 평균 | 축 방향 표준편차 |
|---|---|---|
| P1 (apex) | −0.05mm | **0.59mm** |
| P15 (atlas) | +0.41mm | **2.51mm** |

길이 오차는 P15 축 방향 오차와 거의 1:1로 대응합니다(0097: P15 +3.7 → 길이 −5.0, 1113: +4.8 → −4.5, 1573: −4.4 → +4.2).

P15를 더 잘 찍기 위해 확인한 것들 (전부 15명 정답 기준 LOO):
| 시도 | 결과 |
|---|---|
| 엄지-검지 오목점(W)·엄지 끝점(A) 기준 좌표계에서 P15 위치 | 축 방향 표준편차 **5.2mm**(범위 20mm), 수직 2.3mm → 상수 오프셋으로 MAE 4.5 |
| 전역 기하(A, W, 손 길이 등)로 P15 xy 선형 회귀 | MAE 2.6~3.1mm |
| 길이 자체를 전역 기하로 직접 회귀 | MAE 2.6~3.4mm |
| 관절 비율 P1–P6 : P6–P15 (lnd 정답) | 비율 표준편차 13% (1113: 1.40, 1573: 0.90) → 쓸 수 없음 |
| 엄지 축 단면 폭이 급증하는 접합부 | P15는 접합부보다 10~25mm 더 손목 쪽(평평한 손바닥 안) → 국소 단서 없음 |
| 아틀라스 변형 9종(반경 30/40/50/60, 스케일 유무, 상위 3명만) | P15 축 방향 표준편차 2.15~2.54, 길이 MAE 1.80~2.06 → 차이 없음 |

1113(−4.5)과 1573(+3~5)은 모든 변형에서 같은 방향으로 틀립니다. 이 두 사람은 정답 P15가 다른 사람들의 해부학적 비례와 다르게 놓여 있고, 그 차이가 메쉬 형상에 드러나지 않습니다.

### 오차 최소화 시도 — 방법 블렌드 (효과 없음)
coarse/fine/atlas 예측을 섞는 블렌드(`BLENDS`)를 추가하고 LOO 좌표(`cv_preds_hgb.npz`)로 가중치를 0.1 간격으로 전부 훑었습니다. 세 방법의 P15 오차가 같은 방향이라(아래 표) 섞어도 상쇄되지 않고, atlas 단독이 최선입니다.

| 사람 | coarse | fine | atlas | | 사람 | coarse | fine | atlas |
|---|---|---|---|---|---|---|---|---|
| 0004 | −1.2 | −0.3 | −0.8 | | 1113 | +5.2 | +5.0 | +4.8 |
| 0097 | +6.3 | +6.3 | +3.7 | | 1114 | +1.7 | +2.5 | +2.1 |
| 2633 | −0.3 | +0.5 | +0.9 | | 1115 | +1.8 | +2.1 | +1.9 |
| 2634 | +0.2 | −0.1 | −1.2 | | 1117 | −3.1 | −3.7 | −1.7 |
| 2636 | −1.7 | −1.7 | −0.3 | | 1559 | +3.1 | +4.0 | −1.1 |
| 2637 | +2.8 | +1.4 | +1.0 | | 1573 | −4.1 | −3.7 | −4.4 |

(P15 축 방향 오차, mm. w·coarse + (1−w)·atlas: w=0 → MAE 1.87, w=0.5 → 2.21, w=1 → 2.66.)

각 사람마다 "가장 잘 맞는 방법 쌍"을 고르면 7/12명이 0.5mm 이내지만, 어느 쌍이 맞을지 메쉬만으로는 알 수 없으므로 실제 성능이 아닙니다.

### MLP(딥러닝 계열) 1단계 비교
`--stage1 mlp`(StandardScaler + MLPRegressor 128-128-64, 조기 종료)로 같은 LOO를 실행했습니다(`results_cv_mlp.csv`, `model_mlp.pkl`). 결과는 HGB와 사실상 같습니다. 1단계는 거친 위치만 정하고 최종 P1은 apex, P15는 atlas로 정해지기 때문입니다. 실행 시간은 HGB의 약 5배(총 87분)입니다.

| 1단계 모델 | 12명 LOO MAE (apex/atlas) | 테스트 3명 오차 |
|---|---|---|
| HistGradientBoosting (채택) | 1.87mm | +2.02 / +3.44 / −3.57 |
| MLP 128-128-64 | 1.90mm | +2.17 / +3.43 / −3.46 |

15명 전체 LOO는 따로 돌리지 않았습니다. 12명 LOO + 3명 테스트로 15명 모두가 한 번씩 "학습에 없는 손"으로 예측됐기 때문입니다.

### 12:3 분할을 여러 조합으로 반복하지 않은 이유
반복 분할은 오차를 줄이지 못합니다. 학습 손은 여전히 최대 12명이고, P15 오차는 사람별 해부학 편차라 어느 12명으로 학습하든 같은 방향으로 남습니다. 반복이 알려주는 것은 "테스트 3명 수치가 분할에 따라 얼마나 흔들리는가"뿐인데, 이는 이미 있는 결과로 계산할 수 있습니다.

15명 각각이 학습에 없는 상태로 예측된 길이 오차(apex/atlas, mm):

| 사람 | 오차 | 사람 | 오차 | 사람 | 오차 |
|---|---|---|---|---|---|
| 0004 | +1.07 | 2637 | −0.09 | 1117 | +1.31 |
| 0097 | −4.99 | 1113 | −4.54 | 1559 | +0.45 |
| 2633 | −0.22 | 1114 | −1.91 | 1573 | +4.15 |
| 2634 | +1.30 | 1115 | −2.00 | 0179* | +2.02 |
| 2636 | +0.36 | | | 2635* | +3.44 |
| | | | | 1116* | −3.57 |

(*는 테스트 3명, 12명 학습. 나머지는 LOO, 11명 학습.)

이 15개 오차로 가능한 테스트 3인조 455개 전부의 MAE를 구하면:

| 최소 | 5% | 중앙값 | 95% | 최대 | MAE ≤ 0.5인 3인조 |
|---|---|---|---|---|---|
| 0.22 | 0.76 | **2.07** | 3.52 | 4.56 | 5/455 (1.1%) |

우리 분할(0179, 2635, 1116)의 3.01은 평균보다 나쁜 쪽이었을 뿐입니다. 테스트 3명 수치는 어느 3명을 뽑느냐에 따라 0.2~4.6mm로 흔들리므로, 성능은 **15명 전체 held-out MAE 2.10mm(중앙값 1.91, 0.5mm 이내 4/15)** 로 보고하는 것이 맞습니다. 운 좋은 3인조를 골라 0.5mm를 "달성"하는 것은 의미가 없습니다.

### PLY / STL 파일 확인 (5명분, 도움 안 됨)
`PLY/`, `STL/`에 5명분(0004, 0097, 0179, 1559, 1573)이 있습니다. PLY 헤더는 `x, y, z`와 면 인덱스뿐이고(색·법선·텍스처 없음, rapidform 출력), 정점·면 수와 좌표가 obj와 5×10⁻⁷mm 이내로 동일합니다. STL도 면 수가 같습니다. 즉 같은 메쉬를 다른 확장자로 저장한 것이라 새 정보가 없고, 저장소에서는 제외(`.gitignore`)했습니다.

### 고전 ML(랜덤 포레스트 등) 직접 회귀 확인 (도움 안 됨)
메쉬에서 잰 손 치수 15개(엄지 끝점, 엄지-검지 오목점, 손 길이·너비, 손바닥 폭, 두께 등)로 기준값을 직접 회귀한 15명 LOO: 랜덤 포레스트 3.19, ExtraTrees 3.36, 그래디언트 부스팅 3.48, KNN 3.23, SVR 3.83, Ridge 3.56, 선형 3.07mm. "다른 14명 평균값" 기준선이 3.35mm이므로 어느 것도 기준선과 다르지 않습니다. 학습 알고리즘의 문제가 아니라 형상에 정보가 없다는 뜻입니다.

### P15 정의 변경 검토 (기각)
"SW의 P15를 맞추는 대신, 메쉬에서 규칙으로 계산되는 점 P15′를 엄지 기저로 새로 정의하자"는 방안입니다. 사람이 새로 찍는 게 아니라 코드가 매번 같은 규칙으로 구하므로 재현성은 좋아지지만, 기준값과는 다른 것을 재게 됩니다. 세 후보를 15명 정답 P15와 비교했습니다. t = 메쉬 엄지 끝점에서 엄지 축을 따라 손목 쪽으로 잰 거리.

| 후보 (메쉬만으로 계산) | 규칙 | P15와의 축 방향 차이 (평균 ± 표준편차) |
|---|---|---|
| D1 접합부 | 엄지 축 단면 폭이 25mm를 처음 넘는 지점 | P15가 22.7 ± **8.3**mm 더 손목 쪽 |
| D2 오목점 투영 | 엄지–검지 윤곽 오목점을 엄지 축에 투영 | P15가 9.0 ± **5.2**mm 더 손목 쪽 |
| D3 바닥 들림 최대점 | 엄지 축을 따라 손바닥면(바닥 z)이 유리에서 가장 뜨는 지점 | P15가 5.0 ± **4.9**mm 더 손목 쪽 |
| (참고) 학습 모델 apex/atlas | — | 0.4 ± **2.5**mm |

표준편차가 곧 상수 보정 후에도 남는 사람별 길이 오차입니다. 어느 후보도 ±5mm 이상이라 0.5mm 목표와 무관하고, 학습 모델보다 기준값 일치도가 나쁩니다.

**기각 이유**: 이 길이의 용도는 Size Korea 기준값과의 비교·대체입니다(사용자 확인). 정의를 바꾸면 "우리 정의의 길이를 0.5mm로 재현"은 가능하지만 기준값과 ±5mm 이상 어긋나므로 목적에 맞지 않습니다. 구현하지 않았습니다.

### 특허 KR 10-1217207 "손 자동계측 방법" 규칙 기반 검토 (브랜치 hy1, 2026-09-24)
사용자가 제공한 특허(디엔엠에프티, 2012)는 3D 스캔의 실루엣(외곽선)에서 규칙으로 19개 측정점을 잡습니다: 손목점 → 손목중심 → 방사 외곽선 → 손가락 끝점 → 수지간 구별점(web) → 손가락 축(끝점→web 위 2cm 높이의 폭 이등분점) → 첫째마디중심점(기저) = web 점을 지나 축에 수직인 면 ∩ 축 ∩ 손바닥면. 우리 SW가 이 계열인지 데이터로 검증했습니다.

**구조는 일치합니다.**
| 근거 | 수치 |
|---|---|
| 특허 "손목중심 = 손목가쪽·안쪽점 중간점" | lnd P28 = P26·P27 중점, 오차 0.000 |
| P20~P25는 실루엣 점 | z 4~20mm(손 두께 중간), 재정합 후 표면 위 0.0~0.5mm |
| P21·P22·P23 = 손가락 사이 web | 인접 두 손가락 축에서 각각 ±11~13mm, 표준편차 1mm |
| P20 = 엄지-검지 web | 엄지 축에서 13.0 ± 1.8mm 옆, P25보다 손목중심에 가까움 |
| P25·P24 = 손가쪽점·손안쪽점(추정) | 검지 축 엄지 쪽 12.5mm / 소지 축 바깥 11.7mm |
| P15~P19 = 첫째마디중심점 | 손바닥면 위(z 0~8), 각 손가락 축 위(P1–P6–P15 일직선 1.2°) |
| lnd 좌표계 | P28→P3이 정확히 +y (특허의 손목중심·손끝점 프레임) |

**하지만 P15의 정확한 규칙은 특허 문구와 다릅니다.** 정답 입력(SW의 P1, P15, P20)으로 규칙만 시험한 결과(축 방향 잔차, 15명):
| 규칙 | 평균 ± 표준편차 (최대) |
|---|---|
| 수직 발: P20을 P1–P15 축에 수직 투영 | −0.42 ± **2.37** (5.5) |
| 3D 축·3D 수직면 | +0.88 ± 2.36 |
| 수평선 교점 (lnd 좌표계) | +10.04 ± 1.80 — 좌표계를 −6~+6° 돌려도 sd 1.7~2.4 |
| 고정 비율 (끝점→web 발 거리의 일정 비율) | 비율 sd 0.045 → 2.2mm |
| 저장된 28점으로 만드는 기하 구성 70,192개 전수(수선의 발·직선 교점·상수 오프셋) | 최선 최대 3.4mm (P1–P6 직선 × P20–P22 직선) |
| 손가락 기저 P16~P19 vs 실루엣 web 발 | −3.2 / −4.5 / −6.3 / +1.2 (sd 1.7 / 1.9 / 2.2 / 0.8) |

계산된 점이면 잔차가 0이어야 합니다(P7~14, P28처럼). 어느 규칙도 무너지지 않으므로 SW의 기저점은 저장된 점·실루엣 web·축의 결정적 함수가 아닙니다. 손바닥 높이(z<3)의 web은 존재하지 않습니다(엄지와 손바닥이 한 접촉면이라 notch가 없음).

**파이프라인 재구성 결과** (`thumb_model/rule_pipeline.py`, 메쉬만 입력, 학습 없음; `rule_eval.csv`, `--frame sw`로 SW 좌표계 진단 `rule_eval_swframe.csv`):
| 항목 | 자체 좌표계 | SW 좌표계(P28·P3 사용) |
|---|---|---|
| 손목중심 P28 | 25.8mm (메쉬가 손목에서 절단돼 특허의 오목점이 없음 → 절단면+21mm로 대체) | — |
| 끝점 P1~P5 (3D) | 2.1~3.3mm | 동일 |
| web P21 / P22 / P23 | 1.6 / 1.2 / 2.5mm | 1.5 / 1.2 / 2.0 |
| web P20 (엄지-검지) | 5.5mm (xy 1.5; z는 수직 모서리라 불확정) | 5.1 (xy 1.5) |
| 기저 P16~P18 축 방향 | −3.3 / −4.4 / −6.0 | 동일 |
| **P15 (변형 B: 매질 축 + web 발)** | **−0.9 ± 2.4 (최대 4.8)** | −0.9 ± 2.5 |
| 엄지 길이 MAE (변형 B) | 2.65mm, 0.5mm 이내 0/15 | 2.66 |
| 엄지 축 방향 오차 (변형 B) | +5.2° ± 3.1 | 동일 |

끝점 3mm 오차는 SW의 끝점이 "손목중심에서 가장 먼 점"(특허)이 아니라 축 방향 최원점(apex, 0.6mm)에 가깝기 때문입니다. P15의 재구성 결과(±2.4)가 정답 입력 시험(±2.4)과 같으므로 한계는 web·축 재구성이 아니라 **규칙 자체**입니다.

**결론**: SW는 이 특허 계열이 맞지만 기저점(P15~P19) 규칙은 공개된 문구와 다르고, 메쉬에서 재구성 가능한 어떤 변형도 표준편차 2.4mm 아래로 내려가지 않습니다(학습 모델 2.5mm와 동일 수준). 규칙 기반으로 0.5mm에 가려면 **SW의 실제 기저점 정의**가 필요합니다. 특허가 공개돼 있으므로 공급사에 "첫째마디중심점 정의(문구·상수)"만 문의하는 것이 현실적입니다. 또한 특허의 손목 규칙은 팔 부분이 필요한데 우리 메쉬는 손목에서 절단돼 있어, 절단 전 원본 스캔(해상도 0.3mm)이 있으면 손목·주름 모두에 도움이 됩니다.

### 기준값의 성격
주어진 기준값 "실제(mm)"는 SW의 P1–P15 xy 거리와 15명 모두 ±0.05mm 이내로 같습니다. 즉 **SW 출력값**이며, 캘리퍼 등 수기 실측값은 없습니다(사용자 확인). 따라서 이 작업의 목표는 SW 재현이고, SW 자체가 실제 손 치수 대비 얼마나 정확한지는 이 데이터로 알 수 없습니다.

## 판단
- 이 데이터(15명, 텍스처 없음, 손바닥이 유리판에 눌림)로는 메쉬만으로 기준값 엄지 길이를 0.5mm 안에 맞출 수 없습니다. 현실적 하한은 MAE 약 1.8mm이고, 그 대부분이 P15입니다.
- 0.5mm에 가려면 SW가 P15를 잡을 때 본 것과 같은 입력이 필요합니다.
  1. **주름이 보이는 데이터**: 스캔 텍스처(사진)나 손바닥을 누르지 않은 스캔, SW의 다른 출력 파일이 있으면 MCP 주름을 직접 찾을 수 있습니다.
  2. **훨씬 많은 표본**: 수십~수백 명이면 P15의 사람별 편차 중 형상으로 설명되는 부분을 학습할 여지가 생깁니다. 12명으로는 불가능합니다.
  3. **Size Korea 393 항목의 공식 정의 확인**: P15가 무엇인지 확정되면 어떤 추가 데이터가 결정적인지 정해집니다.
- P15 정의 변경은 위 절에서 검토 후 기각했습니다.

## 실행
```
python align_landmarks.py            # 정합 (aligned/, thumb_length.csv)
python thumb_model/prep.py           # 특징 캐시
python thumb_model/train_eval.py     # 12명 LOO + 3명 테스트 + model_hgb.pkl
python thumb_model/train_eval.py --stage1 mlp     # MLP 1단계 비교
python thumb_model/train_eval.py --loo-all        # 15명 전체 LOO
python thumb_model/predict.py obj/20_F_0179G.obj  # 추론
python thumb_model/rule_pipeline.py               # 특허 규칙 파이프라인 (rule_eval.csv, rule_landmarks.csv)
python thumb_model/rule_pipeline.py --frame sw    # 진단: SW의 P28·P3 좌표계로 실행
```
