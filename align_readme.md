# lnd → obj 랜드마크 정합

## 데이터
- `lnd/`와 `obj/`에 파일명이 같은 15쌍이 있습니다 (예: `20_F_0004G.lnd` ↔ `20_F_0004G.obj`).
- `.lnd`: 한 줄에 `번호 x y z` 형식으로 손 랜드마크 28개가 있습니다 (mm).
  - 실측 3D 점: 1~6, 15~27
  - z가 채워 넣은 값인 점: 7~14 (z=0), 28 (z=1, xy는 P26·P27의 중점)
  - lnd 좌표계에서는 +y가 손목 중심(P28) → 가운데 손가락 끝(P3) 방향입니다.
- `.obj`: rapidform에서 내보낸 손 스캔 메쉬입니다 (mm, 바닥 z≈0, 팔 쪽이 y≈−100에서 잘려 있음). lnd 점은 obj 정점과 정확히 일치하지 않습니다(정점 간격 약 1.6mm).

## 정합 방법 (`align_landmarks.py`)
- **변환**: z축 회전 + xyz 이동만 허용하는 강체 변환입니다 (스케일 없음). 두 데이터 모두 바닥이 z=0이고, 이렇게 하면 xy 평면 측정값(엄지 길이 등)이 바뀌지 않습니다.
- **정합에 쓰는 점**: 실측 3D 점 19개만 씁니다 (`NON_3D_LANDMARKS` = 7~14, 28 제외). 구한 변환은 28개 점 전체에 적용하고, 제외한 점의 z는 원래 값에 z 이동만 더해집니다.
- **알고리즘**
  1. 메쉬 표면에 1mm²당 4개꼴로 점을 샘플링하고 KD-tree를 만듭니다.
  2. z 회전각을 0~360°, 10° 간격으로 초기값으로 두고 각각 ICP를 돌려 전체 점 기준 오차가 가장 작은 해를 고릅니다.
  3. **손 축 방향 다중 시작**: 메쉬만으로 구한 손 축(손목 절단면 중심 → 최대 y 정점, `hand_axis()`)을 따라 −6~+16mm(2mm 간격) 옮긴 시작점에서 다시 ICP를 돌리고, 정합점 19개의 평균 거리가 가장 작은 해를 채택합니다.
  4. ICP는 매 단계 정합점 전부를 씁니다(`TRIM_RATIO = 1.0`). 좌우 반전 해도 계산해서 비교합니다. 15명 모두 반전하지 않은 쪽이 훨씬 잘 맞습니다.
- **점별 오차**: 변환된 점에서 메쉬 표면까지의 최단 거리입니다. 표면을 따라 옆으로 밀린 오차는 잡히지 않습니다.

### 왜 다중 시작이 필요한가 (이전 결과의 문제)
손가락은 축 방향으로 긴 튜브라서 lnd 전체가 손가락 방향으로 몇 mm 미끄러져도 표면 거리는 거의 변하지 않습니다(끝점을 넘어가기 전까지). 이전 버전은 매 단계 가장 안 맞는 15%(19점 중 3점)를 버리는 trimmed ICP를 썼는데, 버려진 점이 정확히 밀어줘야 할 점들(P20~22)이어서 여러 사람(0004, 2633, 2634, 1115 등)에서 손가락 끝 랜드마크가 메쉬 손가락 끝보다 5~9mm 안쪽에 놓였습니다. 이 오차는 표면 거리로는 보이지 않았고, "P1과 메쉬 엄지 끝점의 축 방향 간격"(`tip_apex_gap_mm`)으로 드러났습니다.

### 결과 (15명, 실측 3D 점 19개의 표면 거리)
| 지표 | 이전(trimmed ICP) | 현재 |
|---|---|---|
| 평균 거리 | 0.32~2.77mm | **0.17~0.28mm** |
| 최대 거리 | 최대 8.3mm | **≤0.65mm** |
| P1–엄지 끝점 축 방향 간격 | 0.3~9.2mm (표준편차 2.9) | **0.05~1.83mm (평균 0.9, 표준편차 0.5)** |

3D 회전(x/y축 기울기)까지 허용하는 방식은 xy 엄지 길이를 최대 0.64mm 왜곡해서 쓰지 않았습니다.

## 엄지손가락 길이
- 정의: P1(엄지 끝)과 P15 사이의 xy 평면 유클리드 거리입니다.
- 결과: 15명 모두 측정 기준값(`REFERENCE_THUMB_MM`)과 ±0.05mm 이내로 일치합니다. 정합은 z 회전+이동뿐이라 정합이 바뀌어도 이 값은 변하지 않습니다.
- 저장 위치: `thumb_length.csv` (프로젝트 루트)

## 출력 (`aligned/`)
- `<name>_aligned.lnd`: 변환된 랜드마크입니다. 원본과 같은 형식입니다.
- `<name>_landmarks.ply`: ASCII 색상 점군입니다. 손가락 끝 1~5 빨강, 6~25 초록, 손목 26~28 파랑입니다.
- `<name>_combined.obj`: 원본 메쉬에 랜드마크 구(반지름 1.5mm, 그룹 `landmark_NN`)를 합친 파일입니다.
- `<name>_transform.txt`: 4×4 변환행렬(`X_obj = T @ [X_lnd, 1]`), RMS, 점별 표면 거리가 들어 있습니다.
- `summary.csv`: 정합점 RMS·평균·최대 거리, `tip_apex_gap_mm`, z 회전각, 반전 RMS, 엄지 길이를 담은 요약입니다.

변환 후 각 사람은 **자기 obj 좌표계**에 놓입니다. 15명이 x/y축을 공유하지는 않습니다(z축은 공통). 공통 좌표계가 필요하면 `hand_axis()`로 만든 손 축을 쓰면 됩니다(`thumb_model/prep.py`가 이 방식을 씁니다).

## 실행
```
python align_landmarks.py
```
원본 `lnd/`와 `obj/`는 수정하지 않습니다.
