# lnd → obj 랜드마크 정합

## 데이터
- `lnd/`와 `obj/`에 파일명이 같은 15쌍이 있습니다 (예: `20_F_0004G.lnd` ↔ `20_F_0004G.obj`).
- `.lnd`: 한 줄에 `번호 x y z` 형식으로 손 랜드마크 28개가 있습니다 (mm).
  - 실측 3D 점: 1~6, 15~27
  - z가 채워 넣은 값인 점: 7~14 (z=0), 28 (z=1, xy는 P26·P27의 중점)
  - lnd 좌표계에서는 +y가 손목 중심(P28) → 가운데 손가락 끝(P3) 방향입니다.
- `.obj`: rapidform에서 내보낸 손 스캔 메쉬입니다 (mm, 바닥 z≈0, 팔 쪽이 y≈−100에서 잘려 있음). lnd 점은 obj 정점과 정확히 일치하지 않습니다.

## 정합 방법 (`align_landmarks.py`)
- **변환**: z축 회전 + xyz 이동만 허용하는 강체 변환입니다 (스케일 없음). 두 데이터 모두 바닥이 z=0이고, 이렇게 하면 xy 평면 측정값(엄지 길이 등)이 바뀌지 않습니다.
- **정합에 쓰는 점**: 실측 3D 점만 씁니다 (`NON_3D_LANDMARKS` = 7~14, 28 제외). 구한 변환은 28개 점 전체에 적용하고, 제외한 점의 z는 원래 값에 z 이동만 더해집니다.
- **알고리즘**
  1. 메쉬 표면에 1mm²당 4개꼴로 점을 샘플링하고 KD-tree를 만듭니다.
  2. z 회전각을 0~360°, 10° 간격으로 초기값으로 두고 각각 trimmed ICP를 돌립니다. 매 단계 가까운 85%의 대응점만 씁니다.
  3. 전체 점 기준 오차가 가장 작은 해를 고른 뒤 정밀화합니다.
  4. 좌우 반전 해도 계산해서 비교합니다. 15명 모두 반전하지 않은 쪽이 더 잘 맞았습니다.
- **점별 오차**: 변환된 점에서 메쉬 표면까지의 최단 거리입니다. 표면을 따라 옆으로 밀린 오차는 잡히지 않습니다.

### 비교한 정합 방식 (15명, 실측 3D 점 19개의 표면 거리)
| 방식 | 평균 | 중앙값 | RMS | 손가락 끝 1~5 평균 |
|---|---|---|---|---|
| 28개 점 전부 사용 | 1.87 | 1.05 | 2.82 | 1.17 |
| **가짜 z 점 제외 (채택)** | **1.30** | **0.65** | **2.05** | **0.92** |
| 여기에 P20~23도 제외 | 2.82 | 1.52 | 4.04 | 1.66 |

단위는 mm입니다. 3D 회전(x/y축 기울기)까지 허용하는 방식은 xy 엄지 길이를 최대 0.64mm 왜곡해서 쓰지 않았습니다.

**참고**: P20~23은 여러 사람(1115, 2634, 0004 등)에서 손 안쪽으로 3~8mm 들어가 있습니다. 하지만 정합에서 빼면 전체가 불안정해지므로 포함했습니다.

## 엄지손가락 길이
- 정의: P1(엄지 끝)과 P15 사이의 xy 평면 유클리드 거리입니다.
- 결과: 15명 모두 측정 기준값(`REFERENCE_THUMB_MM`)과 ±0.05mm 이내로 일치합니다.
- 저장 위치: `thumb_length.csv` (프로젝트 루트)

## 출력 (`aligned/`)
- `<name>_aligned.lnd`: 변환된 랜드마크입니다. 원본과 같은 형식입니다.
- `<name>_landmarks.ply`: ASCII 색상 점군입니다. 손가락 끝 1~5 빨강, 6~25 초록, 손목 26~28 파랑입니다.
- `<name>_combined.obj`: 원본 메쉬에 랜드마크 구(반지름 1.5mm, 그룹 `landmark_NN`)를 합친 파일입니다.
- `<name>_transform.txt`: 4×4 변환행렬(`X_obj = T @ [X_lnd, 1]`), RMS, 점별 표면 거리가 들어 있습니다.
- `summary.csv`: RMS, 정합에 쓴 점의 최대 거리, z 회전각, 반전 RMS, 엄지 길이를 담은 요약입니다.

변환 후 각 사람은 **자기 obj 좌표계**에 놓입니다. 15명이 x/y축을 공유하지는 않습니다(z축은 공통).

## 실행
```
python align_landmarks.py
```
원본 `lnd/`와 `obj/`는 수정하지 않습니다.
